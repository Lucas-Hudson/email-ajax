<p id="<%= @task.id %>"> <%= @task.title %>, to read: <%= @task.deadline.strftime('%D') %>
  <%= link_to task_path(@task.id), method: :delete, remote: true do %>
  <i class="fas fa-trash-alt"></i>
  <% end %>
</p>
